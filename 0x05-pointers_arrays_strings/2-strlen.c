#include <stdio.h>
#include <string.h>
/**
 * _strlen - returns the length of a string
 *
 * @s: Pointer to the string.
 * Return: the lenght of the string.
 */
int _strlen(char *s)
{
	return (strlen(s));
}
